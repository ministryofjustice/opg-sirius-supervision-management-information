const nonReportingUser = "1"
const reportingUser = "2"

describe("Role-based permissions", () => {
//    it("checks permissions for user with no reporting roles", () => {
//        cy.setCookie("x-test-user-id", nonReportingUser);
//        cy.visit("/downloads");
//        cy.contains(".moj-button-menu__item", "Edit payment method").should("not.exist");
//
//        cy.visit("/uploads");
//        cy.contains(".govuk-button", "Award fee reduction").should("not.exist");
//        cy.contains(".govuk-button", "Cancel").should("not.exist");
//
//    });
//
//    it("checks permissions for reporting user role", () => {
//        cy.setCookie("x-test-user-id", reportingUser);
//        cy.visit("/downloads");
//        cy.contains(".moj-button-menu__item", "Edit payment method").should("exist");
//
//        cy.visit("/uploads");
//        cy.contains(".govuk-button", "Award fee reduction").should("exist");
//        cy.contains(".govuk-button", "Cancel").should("exist");
//    });
});

